apiVersion: v1
kind: Service
metadata:
  name: {{ include "arch-delugevpn.fullname" . }}
  labels:
    {{- include "arch-delugevpn.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arch-delugevpn.selectorLabels" . | nindent 4 }}
